import SwiftUI

// Kept for compatibility — placeholder

struct TaskDetail: View {
    let taskId: String

    var body: some View {
        Text("Task \(taskId)")
            .foregroundColor(.secondary)
    }
}
